#include <stdio.h>
int main() {
   //program to add two numbers
   int a,b,sum;
   printf("Enter two numbers :");
   scanf("%d %d",&a,&b);
   sum = a + b;
   printf("\n Sum of the two numbers :%d",sum);
    return 0;
}